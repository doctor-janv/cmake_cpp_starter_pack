# cmake_cpp_starter_pack
A basic cmake c++ starter pack

Simply run `./configure.py -d <AppDir> -n <AppName>` where `<AppDir>` is a directory where you want to host your application and `<AppName>` is the name of your app.
